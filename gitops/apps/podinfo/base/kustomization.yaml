apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: podinfo
  namespace: flux-system

labels:
  - pairs:
      app: podinfo

resources:
  - deployment.yaml
  - service.yaml